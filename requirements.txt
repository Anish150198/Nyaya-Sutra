# Python 3.12
# conda create --name nb python=3.12
# conda activate nb
# pip install -r requirements.txt

# Core framework
streamlit>=1.30.0
pydantic>=2.5.0

# OpenAI (LLM + Embeddings) — primary provider
openai>=1.0.0

# Vector search — ChromaDB (local persistent)
chromadb>=0.4.0

# Databricks Vector Search (used when VECTOR_DB=databricks_vs)
databricks-vectorsearch>=0.22

# Delta Lake (local medallion tables)
deltalake>=0.15.0
pyarrow>=14.0.0

# MLflow & evaluation
mlflow[databricks]>=2.12.0
ragas>=0.1.7

# Databricks connectivity
databricks-sdk>=0.20.0

# Data processing
pandas>=2.1.0
numpy>=1.24.0

# NLP utilities
tiktoken>=0.5.0
langdetect>=1.0.9

# Misc
python-dotenv>=1.0.0
PyYAML>=6.0
requests>=2.31.0

fpdf==1.7.2
google.generativeai


